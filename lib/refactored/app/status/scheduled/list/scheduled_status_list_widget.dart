import 'package:flutter/cupertino.dart';

class ScheduledStatusListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Scheduled list"));
  }
}
